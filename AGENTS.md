## Workspace Notes
- The user always removes existing PowerClimate entries before reconfiguring, so migrations or legacy compatibility paths are unnecessary.
- my home assistant server can be reached via drive Z:\ 
- when in Planning mode do not focus on HOW to make the changes but help brainstorming about the requirements (the WHAT). Assume the implementing LLM model will be smarter than the planning  LLM model.  