## Workspace Notes
- PowerClimate currently assumes a clean configuration and does not include migration logic for legacy entries.
- Keep user documentation generic (no local paths, no private server references).