[tool.black]
line-length = 100
target-version = ["py310", "py311"]

[tool.ruff]
line-length = 100
select = ["E", "F", "I", "B", "W", "UP"]
ignore = ["B008"]
exclude = [".git", ".venv", "__pycache__"]

[tool.pytest.ini_options]
asyncio_mode = "auto"
addopts = "-q"
